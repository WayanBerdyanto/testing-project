pipeline {
    agent any

    stages {
        stage('Auto Pull from GitHub') {
            steps {
                echo '[1] Pulling latest code from main branch...'
                bat 'git pull origin main'
                echo '[2] Pull complete ✅'
            }
        }
    }
}
